# ATM Cash Intelligence Frontend

React/Vite dashboard for ATM cash monitoring, depletion forecasts, and refill
recommendations.

## Run

```bash
cd frontend
npm install
npm run dev
```

Set the backend URL with:

```bash
cp .env.example .env
```

Default API URL:

```text
http://localhost:8000
```
