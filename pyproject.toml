[project]
name = "atm-cash-demand-predictor"
version = "0.1.0"
description = "Time-aware ATM cash withdrawal demand forecasting pipeline"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "joblib>=1.3.0",
    "numpy>=1.26.0",
    "pandas>=2.2.0",
    "scikit-learn>=1.4.0",
]
